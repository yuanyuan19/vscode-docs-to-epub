# ==================== LLM 提供商选择 ====================
# 可选值: openai | anthropic | custom
LLM_PROVIDER=custom

# ==================== OpenAI 配置 ====================
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o
OPENAI_BASE_URL=  # 可选，自定义 API 地址，如: https://api.openai.com/v1
OPENAI_TEMPERATURE=0.3
OPENAI_MAX_TOKENS=4000

# ==================== Anthropic 配置 ====================
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
ANTHROPIC_BASE_URL=  # 可选，自定义 API 地址
ANTHROPIC_MAX_TOKENS=4096

# ==================== 自定义 API 配置 ====================
# 当 LLM_PROVIDER=custom 时使用
CUSTOM_API_URL=xx
CUSTOM_API_KEY=xx
CUSTOM_MODEL=xx

# API 格式: openai | anthropic | custom
# 如果使用 openai 格式，响应格式为: { choices: [{ message: { content: "..." } }] }
# 如果使用 anthropic 格式，响应格式为: { content: [{ text: "..." }] }
# 如果使用 custom，需要设置 CUSTOM_API_BODY 和 CUSTOM_API_RESPONSE_PATH
CUSTOM_API_FORMAT=openai

# 自定义请求体（JSON 字符串，仅在 CUSTOM_API_FORMAT=custom 时使用）
CUSTOM_API_BODY={}

# 自定义请求头（JSON 字符串）
CUSTOM_API_HEADERS={}

# 自定义响应路径（用于提取翻译结果，如: data.choices[0].message.content）
CUSTOM_API_RESPONSE_PATH=data.choices[0].message.content

# 自定义参数
CUSTOM_TEMPERATURE=0.3
CUSTOM_MAX_TOKENS=4000

# ==================== 翻译配置 ====================
TARGET_LANGUAGE=zh-CN

# ==================== 并发配置 ====================
CONCURRENT_FILES=40        # 同时翻译的文件数
CONCURRENT_CHUNKS=4       # 每个文件内同时翻译的块数
DELAY_BETWEEN_FILES=500   # 文件间延迟（毫秒）
DELAY_BETWEEN_CHUNKS=200  # 块间延迟（毫秒）